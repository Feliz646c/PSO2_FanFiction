# AGENTS.md

## 1. このリポジトリについて

このリポジトリは、『PHANTASY STAR ONLINE 2（PSO2）』を原作とした独立二次創作プロジェクトの制作・設定管理リポジトリである。

AIエージェントは、設定整理、アイデア検討、整合性確認、セッション構想、小説執筆補助、資料更新などを行う。

本プロジェクトでは、単に文章として自然であることよりも、以下を重視する。

- ユーザーが確定した設定との整合性
- キャラクターの思想・人格との整合性
- 時系列の整合性
- 出来事の因果関係
- 世界設定上のルールとの整合性
- 原作設定と本作独自設定の区別
- 未確定案を確定設定として扱わないこと

---

# 2. 最重要原則

## 2.1 ユーザー確定設定を正史とする

ユーザーが明示的に確定した本作独自設定を、このプロジェクトにおける正史（Canon）として扱う。

以下のようなユーザーの明示的な意思表示があった場合、その内容は確定設定として扱ってよい。

- 「確定しましょう」
- 「これで確定」
- 「この方向で決定」
- 「設定マスターに反映してよい」
- 「正史として扱う」
- その他、明確に採用・確定を示す発言

AIが提案しただけの内容は、ユーザーが承認するまで確定設定ではない。

---

## 2.2 設定ステータスを厳格に区別する

設定は原則として、以下の状態を区別する。

### 【確定設定】

ユーザーによって明示的に確定された設定。

正史として扱う。

既存の【確定設定】を変更・削除・上書きしてはならない。

変更が必要と思われる場合は、編集する前にユーザーへ矛盾点または変更理由を提示すること。

### 【確定方向】

大筋や方向性は確定しているが、名称・詳細・演出などに未決事項が残っている状態。

確定済み部分を変更してはならない。

未決部分については提案可能。

### 【有力案】

採用可能性が高い案。

確定設定ではない。

他の設定を検討する際の材料として参照してよいが、前提条件として固定してはならない。

### 【未決事項】

まだ結論が出ていない事項。

自由に提案・比較・検討してよい。

ただし、ユーザーの明示的な承認なしに【確定設定】へ昇格させてはならない。

---

# 3. 情報の優先順位

設定に矛盾または差異が存在する場合、原則として以下の優先順位で判断する。

1. ユーザーが現在の会話で明示的に確定した最新設定
2. `canon/` 内の最新の確定設定
3. 本プロジェクト内の、より新しい確定資料
4. セッション資料・キャラクター資料等に記載された確定情報
5. 有力案・構想資料
6. PSO2原作設定
7. AIによる推測・補完

上位情報と下位情報が矛盾する場合、上位情報を優先する。

AI自身の推測によって既存設定を上書きしてはならない。

---

# 4. PSO2原作設定の扱い

本作はPSO2を原作とした二次創作であるが、原作世界そのものではない。

原作設定と本作設定が一致するとは限らない。

本作独自設定とPSO2原作設定が異なる場合は、必ず本作設定を優先する。

原作知識を用いて設定を補完する場合は、それが本作で確定している情報なのか、原作由来の情報なのかを区別する。

原作設定を根拠として、本作の確定設定を自動的に修正してはならない。

---

# 5. 他プロジェクトの設定を持ち込まない

過去に制作された別のPSO2二次創作、旧企画、別世界線、その他の創作プロジェクトの設定を、本プロジェクトへ持ち込んではならない。

名前や人物が類似していても、同一設定であるとは判断しない。

本リポジトリおよび現在のプロジェクト内で確認できない設定については、不明として扱う。

---

# 6. Canonファイルの保護

`canon/` は本プロジェクトにおける正史情報を格納する領域である。

原則として、ユーザーから明示的な反映指示が出るまで `canon/` を編集してはならない。

例えば、

「この案を考えたい」
「設定を検討したい」
「候補を出して」
「どう思う？」
「整理してみて」

といった依頼は、`canon/` の変更許可を意味しない。

一方、

「これで確定」
「設定マスターに反映して」
「canonに追加して」
「この内容で更新して」

などの明示的な指示があった場合は編集してよい。

判断できない場合は編集しない。

---

# 7. 相談フェーズと反映フェーズ

創作作業は原則として、以下の二段階に分ける。

## Phase A: 相談・検討

設定案、名称案、ストーリー案、キャラクター思想、因果関係などをユーザーと検討する。

この段階では、原則としてCanonファイルを変更しない。

必要に応じて既存資料を読み、既存設定との整合性を確認する。

## Phase B: 確定・反映

ユーザーが案を明示的に確定した後、その内容を適切な設定ファイルへ反映する。

反映時には、会話中に出たすべての案ではなく、最終的に確定された内容だけを記録する。

検討途中で却下された案を混入させてはならない。

---

# 8. 設定変更時の整合性確認

設定を追加・変更する場合、関連する既存設定を確認する。

特に以下を重点的に確認する。

- キャラクターの年齢
- 生年・年代
- 時系列
- 所属組織
- 人間関係
- 能力とその制約
- 過去の出来事
- 行動動機
- 思想・価値観
- 世界設定上の法則
- 記憶継承などの特殊ルール
- 出来事同士の因果関係

矛盾を発見した場合、勝手に修正して辻褄を合わせてはならない。

ユーザーへ、

- どの設定とどの設定が矛盾しているか
- なぜ矛盾すると判断したか
- 解決方法として何が考えられるか

を提示する。

最終的な選択はユーザーに委ねる。

---

# 9. キャラクター設定の扱い

キャラクターの行動を検討する際は、「ストーリー上そうすると都合がよい」という理由だけで行動させない。

以下の順序で考える。

1. その人物は何を知っているか
2. その人物は何を知らないか
3. その人物は何を望んでいるか
4. その人物は何を恐れているか
5. その人物の思想・価値観では何を選ぶか
6. その選択によって何が起きるか

作者視点の情報とキャラクター視点の情報を混同しない。

キャラクターが知ることのできない情報を理由に行動させてはならない。

---

# 10. 因果関係

物語上の出来事については、

原因
→ 判断
→ 行動
→ 結果
→ 次の出来事

という因果関係を重視する。

「物語をその展開へ進めたいから」という理由だけで、キャラクターや世界設定を不自然に動かさない。

既存設定から自然にその結果へ到達できない場合は、その不足している因果を指摘する。

---

# 11. 資料ごとの役割

原則として以下の役割分担を使用する。

## `canon/`

確定設定。

世界設定、キャラクター設定、用語、能力、組織など、今後の創作において前提となる情報を格納する。

変更にはユーザーの明示的な確定・反映指示を必要とする。

## `sessions/`

セッション構想、シナリオ進行、登場人物配置、イベント、演出案など。

確定設定だけでなく、確定方向・有力案・未決事項を含む場合がある。

資料内でステータスを明確にする。

## `drafts/`

アイデア出し、検討中設定、没案候補、試作文章など。

自由に編集可能。

ここに記載されているだけでは正史とはみなさない。

## `stories/`

小説・小話などの本文。

本文として確定した内容であっても、それだけを理由に世界設定マスターの記述を自動変更しない。

本文から新しい設定が確定したと判断できる場合でも、Canonへの反映はユーザーの指示を待つ。

---

# 12. Git運用

## 12.1 mainを直接編集しない

原則として `main` ブランチ上でファイルを編集・コミットしない。

作業開始時に目的別のブランチを作成する。

例：

```text
work/casino-session
work/roa-profile
work/ral-profile
work/lust-character
work/ep5-restructure
fix/canon-contradiction-roa
docs/update-world-master
```

---

## 12.2 一つのブランチに複数テーマを混在させない

原則として、

**1テーマ = 1ブランチ**

とする。

例えば「カジノ編の構想変更」と「ロアの人物設定変更」が独立した変更である場合、別ブランチとして扱う。

ただし、一つの設定変更によって複数資料を同期更新する必要がある場合は、同一ブランチで扱ってよい。

---

## 12.3 作業開始前の確認

ファイル編集を伴う作業を開始する前に、可能な範囲で以下を確認する。

```bash
git status
git branch --show-current
```

未コミットの変更が存在する場合、その変更を勝手に破棄・上書き・stashしてはならない。

既存変更と今回の作業が衝突する可能性がある場合はユーザーへ報告する。

---

## 12.4 コミット

ユーザーからコミットを依頼されるまで、原則としてコミットしない。

コミットする場合は、変更内容を表す簡潔なメッセージを使用する。

例：

```text
docs: ロアの確定設定を更新
docs: カジノ編の構想を更新
fix: 年代設定の矛盾を修正
docs: 用語マスターに新規用語を追加
```

---

## 12.5 Push / Merge

ユーザーから明示的な指示がない限り、リモートへのPush、Pull Request作成、Mergeを行わない。

特に `main` へのMergeは、ユーザーの確認を必要とする。

---

# 13. ファイル編集時の原則

既存ファイルを編集する場合、必要な箇所だけを変更する。

以下を禁止する。

- 指示されていない大規模な文章整形
- 意味を変えない文章の無差別な書き換え
- 見出し構造の不要な変更
- ユーザーが意図して残している記述の削除
- 未確定設定の確定化
- 確定設定の推測による補完
- 関係のない設定の「ついで修正」

改善できそうな箇所を発見しても、今回の作業範囲外なら原則として変更しない。

必要であれば作業完了報告時に指摘する。

---

# 14. 不明情報の扱い

既存資料から確認できない情報について、もっともらしい内容を捏造して補完してはならない。

以下を区別する。

**資料から確認できる事実**
: 確定情報として扱う。

**既存設定から論理的に推測できること**
: 推測であることを明示する。

**複数の可能性が存在すること**
: 候補として提示する。

**判断材料が存在しないこと**
: 未決または不明として扱う。

「おそらくこうだろう」という推測をCanonへ記録してはならない。

---

# 15. 新しい設定を提案する場合

新規設定の提案は歓迎される。

ただし、既存設定との区別を明確にする。

提案時には必要に応じて、

- 既存設定から自然に導ける案
- 既存設定との相性が良い案
- 意図的に方向性を変える案

などを区別する。

AIが特定案を推奨することは可能だが、最終決定はユーザーが行う。

---

# 16. 作業完了時の報告

ファイルを変更した場合、作業終了時に簡潔な変更報告を行う。

最低限、以下を報告する。

```text
変更ファイル:
- canon/01_キャラクター設定マスター.md
- canon/02_用語マスター.md

変更内容:
- ○○の確定設定を追加
- ○○の用語定義を追加

設定ステータス:
- 確定設定のみ反映
- 未決事項は変更なし

Git:
- Branch: work/xxxxx
- Commit: 未実施
- Push: 未実施
```

矛盾・懸念事項を発見した場合は、それも別途報告する。

---

# 17. 禁止事項

AIエージェントは以下を行ってはならない。

1. ユーザーの承認なしに提案を確定設定へ昇格する。
2. 本作設定よりPSO2原作設定を優先する。
3. 他の二次創作・旧企画の設定を混入する。
4. 矛盾を発見した際、独断で既存設定を書き換える。
5. `main` に直接変更をコミットする。
6. ユーザーの未コミット変更を勝手に破棄する。
7. 作業範囲外のファイルを「ついでに」変更する。
8. Canonの不足部分をAIの推測だけで補完する。
9. 却下された案を確定資料へ残す。
10. ユーザーの指示なしにmainへMergeする。

---

# 18. 判断に迷った場合

設定の状態、編集権限、ユーザーの意図、既存設定との整合性について判断できない場合は、

**変更しないことを優先する。**

そのうえで、判断に必要な情報をユーザーへ確認する。

本プロジェクトでは、

**誤った設定を追加することより、設定を追加せず確認を求めることを優先する。**

---

# 19. 基本作業フロー

通常の創作作業では、以下の順序を基本とする。

```text
ユーザーから依頼
        ↓
関連資料を確認
        ↓
既存の確定設定を把握
        ↓
矛盾・関連事項を確認
        ↓
相談・提案
        ↓
ユーザーによる選択・確定
        ↓
反映指示の確認
        ↓
対象ファイルのみ編集
        ↓
差分確認
        ↓
変更内容をユーザーへ報告
        ↓
ユーザー確認
        ↓
Commit / Push / PR
        ↓
最終確認後にmainへMerge
```

創作上の「確定」と、Git上の「反映・Commit・Merge」は別の操作として扱う。

ユーザーが設定を「確定」と発言しただけでは、Git操作まで許可されたとは解釈しない。
